// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    /* config options here */
    output: 'export',
    reactStrictMode: true,
    basePath: '/home'
}

export default nextConfig